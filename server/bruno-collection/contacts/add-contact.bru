meta {
  name: add-contact
  type: http
  seq: 2
}

post {
  url: https://localhost:3003/api/contacts/
  body: json
  auth: bearer
}

auth:bearer {
  token: {{voiceAccessToken}}
}

body:json {
  {
    "email": "noldya@gmail.com"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
