meta {
  name: calls
  type: http
  seq: 3
}

post {
  url: https://localhost:3003/api/calls/
  body: json
  auth: bearer
}

auth:bearer {
  token: {{voiceAccessToken}}
}

body:json {
  
  {
    "targetUserId": "{{voiceUserId}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
