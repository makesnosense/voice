meta {
  name: cancel invite to room
  type: http
  seq: 4
}

post {
  url: https://localhost:3003/api/rooms/{{voiceRoomId}}/cancel-invite
  body: json
  auth: bearer
}

auth:bearer {
  token: {{voiceAccessToken}}
}

settings {
  encodeUrl: true
  timeout: 0
}
