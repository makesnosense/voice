meta {
  name: invite-to-room
  type: http
  seq: 7
}

post {
  url: https://localhost:3003/api/rooms/{{voiceRoomId}}/invite
  body: json
  auth: bearer
}

auth:bearer {
  token: {{voiceAccessToken}}
}

body:json {
  
  {
    "targetEmail": "noldya@gmail.com"
  } 
}

settings {
  encodeUrl: true
  timeout: 0
}
