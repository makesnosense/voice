meta {
  name: create-room
  type: http
  seq: 1
}

post {
  url: https://localhost:3003/api/rooms
  body: json
  auth: bearer
}

auth:bearer {
  token: 
}

script:post-response {
  const response = res.getBody();
  if (response && response.roomId) {
    bru.setVar("voiceRoomId", response.roomId);
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
