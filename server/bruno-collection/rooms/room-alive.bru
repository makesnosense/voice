meta {
  name: room-alive
  type: http
  seq: 2
}

get {
  url: https://localhost:3003/api/rooms/{{voiceRoomId}}/alive
  body: json
  auth: bearer
}

auth:bearer {
  token: {{voiceAccessToken}}
}

settings {
  encodeUrl: true
  timeout: 0
}
