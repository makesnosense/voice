meta {
  name: register
  type: http
  seq: 1
}

post {
  url: https://localhost:3003/api/devices/
  body: json
  auth: inherit
}

body:json {
  {
    "refreshToken": "{{voiceRefreshToken}}",
    "platform": "web",
    "deviceName": "test-device21"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
