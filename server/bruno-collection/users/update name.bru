meta {
  name: update name
  type: http
  seq: 2
}

patch {
  url: https://localhost:3003/api/users/me
  body: json
  auth: bearer
}

auth:bearer {
  token: {{voiceAccessToken}}
}

body:json {
  {"name": "Updated Name"}
}

settings {
  encodeUrl: true
  timeout: 0
}
