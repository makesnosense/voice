meta {
  name: delete my account
  type: http
  seq: 7
}

delete {
  url: https://localhost:3003/api/users/me
  body: json
  auth: inherit
}

body:json {
  {
    "refreshToken": "{{voiceRefreshToken}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
