meta {
  name: answer call
  type: http
  seq: 6
}

post {
  url: https://localhost:3003/api/calls/{{voiceCallId}}/mark-answered
  body: none
  auth: bearer
}

auth:bearer {
  token: {{voiceAccessToken}}
}

settings {
  encodeUrl: true
  timeout: 0
}
