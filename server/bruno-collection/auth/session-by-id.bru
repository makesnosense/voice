meta {
  name: session-by-id
  type: http
  seq: 6
}

delete {
  url: https://localhost:3003/api/auth/sessions/{{jti}}
  body: json
  auth: inherit
}

body:json {
  {
    "refreshToken": "{{voiceRefreshToken}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
