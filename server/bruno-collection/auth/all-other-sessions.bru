meta {
  name: all-other-sessions
  type: http
  seq: 4
}

delete {
  url: https://localhost:3003/api/auth/sessions
  body: json
  auth: inherit
}

body:json {
  {
    "refreshToken": "{{voiceRefreshToken}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
