meta {
  name: verify-otp
  type: http
  seq: 2
}

post {
  url: https://localhost:3003/api/auth/verify-otp
  body: json
  auth: inherit
}

body:json {
  {
    "email": "berezovskyaa@gmail.com",
    "code": "703719"
  }
}

script:post-response {
  const response = res.getBody();
  if (response && response.accessToken) {
    bru.setVar("voiceAccessToken", response.accessToken);
    bru.setVar("voiceRefreshToken", response.refreshToken);
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
